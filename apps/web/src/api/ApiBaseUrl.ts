/**
 * Where the API lives.
 *
 * Used both by the generated client's `baseURL` and by the few places that
 * build a plain browser URL by hand — a resume file, for example, is served as
 * a static path (`/uploads/...`) that the browser fetches directly rather than
 * through the client.
 */
export const API_BASE_URL = "http://localhost:3000"
